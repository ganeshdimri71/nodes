import React from 'react'

const Button = () => {
   const changePointsHandler = () => {
        
    }
  return (
    <div>
      <button onClick={changePointsHandler}>Change The Points</button>
    </div>
  )
}

export default Button
